# Harsh survival test

Generated by `harsh_verify.py`. Results only; nothing here is proved.

## Method

- Pool: the **389** census games carrying a residue law
- Sample: **20** games, `random.Random(13).sample(...)` over the
  pool sorted by (modulus, m, r, branch order, F)
- Each law tested to N = 1,000,000
- Win/loss only: one P/N bit per position, **no Grundy values computed**
- Move sets from `compile_rule`; the scan over a position's moves
  early-exits on the first move reaching a P-position
- No OEIS calls

Sample composition: the pool is 375 `squares / F` and 14 `F / squares`; this sample drew 20 and 0 respectively.

## Result

- Laws holding to 10^6: **20 / 20**
- Laws with a counterexample below 10^6: **0 / 20**

| DSL | modulus | residues | preperiod | result |
|---|---:|---|---:|---|
| `["ifmod", 5, 1, ["squares"], ["range", 1, 4]]` | 5 | 0 | 1 | holds to 10^6 |
| `["ifmod", 5, 2, ["squares"], ["const", [3]]]` | 5 | 0, 1 | 1 | holds to 10^6 |
| `["ifmod", 3, 0, ["squares"], ["const", [9]]]` | 18 | 1, 2, 4, 5, 7, 8 | 16 | holds to 10^6 |
| `["ifmod", 3, 1, ["squares"], ["const", [5]]]` | 3 | 0 | 3 | holds to 10^6 |
| `["ifmod", 8, 0, ["squares"], ["range", 1, 2]]` | 16 | 1, 4, 7, 10, 13 | 15 | holds to 10^6 |
| `["ifmod", 5, 0, ["squares"], ["const", [1, 6, 9]]]` | 5 | 2, 4 | 20 | holds to 10^6 |
| `["ifmod", 8, 3, ["squares"], ["const", [8]]]` | 16 | 0, 1, 2, 4, 5, 6, 7 | 1 | holds to 10^6 |
| `["ifmod", 8, 4, ["squares"], ["const", [5]]]` | 2 | 1 | 16 | holds to 10^6 |
| `["ifmod", 8, 5, ["squares"], ["const", [2]]]` | 4 | 0, 3 | 4 | holds to 10^6 |
| `["ifmod", 4, 0, ["squares"], ["const", [8]]]` | 16 | 1, 2, 3, 5, 6, 7 | 1 | holds to 10^6 |
| `["ifmod", 8, 2, ["squares"], ["const", [1, 6, 9]]]` | 32 | 0, 5, 7, 12, 15, 17, 19, 22, 27, 29 | 35 | holds to 10^6 |
| `["ifmod", 8, 2, ["squares"], ["const", [3]]]` | 2 | 1 | 16 | holds to 10^6 |
| `["ifmod", 4, 3, ["squares"], ["const", [5]]]` | 2 | 0 | 23 | holds to 10^6 |
| `["ifmod", 5, 0, ["squares"], ["range", 3, 6]]` | 10 | 6, 7, 8 | 20 | holds to 10^6 |
| `["ifmod", 8, 1, ["squares"], ["range", 2, 2]]` | 4 | 0, 3 | 1 | holds to 10^6 |
| `["ifmod", 7, 4, ["squares"], ["range", 3, 6]]` | 35 | 0, 1, 2, 9, 10, 17, 19, 26, 27, 28 | 1 | holds to 10^6 |
| `["ifmod", 5, 2, ["squares"], ["const", [6]]]` | 5 | 0, 3 | 17 | holds to 10^6 |
| `["ifmod", 2, 1, ["squares"], ["const", [1]]]` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 7, 3, ["squares"], ["const", [3]]]` | 7 | 4, 5, 6 | 15 | holds to 10^6 |
| `["ifmod", 8, 1, ["squares"], ["const", [7]]]` | 2 | 0 | 27 | holds to 10^6 |

## Per game

### `["ifmod", 5, 1, ["squares"], ["range", 1, 4]]`

- Rule: subtract s ∈ [if n≡1 (mod 5): squares ≤ n else: {1..4}]
- game_hash: `8df506dcb504`
- Tested to N = 1,000,000 in 3.8s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 5).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 5, 2, ["squares"], ["const", [3]]]`

- Rule: subtract s ∈ [if n≡2 (mod 5): squares ≤ n else: {3}]
- game_hash: `1a06c7135e24`
- Tested to N = 1,000,000 in 3.7s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 1 (mod 5).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 3, 0, ["squares"], ["const", [9]]]`

- Rule: subtract s ∈ [if n≡0 (mod 3): squares ≤ n else: {9}]
- game_hash: `cdec9928baaa`
- Tested to N = 1,000,000 in 5.9s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 16, the P-positions are exactly n ≡ 1, 2, 4, 5, 7, 8 (mod 18).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 3, 1, ["squares"], ["const", [5]]]`

- Rule: subtract s ∈ [if n≡1 (mod 3): squares ≤ n else: {5}]
- game_hash: `5e1db8a2b7ed`
- Tested to N = 1,000,000 in 6.0s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 3, the P-positions are exactly n ≡ 0 (mod 3).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 0, ["squares"], ["range", 1, 2]]`

- Rule: subtract s ∈ [if n≡0 (mod 8): squares ≤ n else: {1..2}]
- game_hash: `d6f33f239acc`
- Tested to N = 1,000,000 in 3.2s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 15, the P-positions are exactly n ≡ 1, 4, 7, 10, 13 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 5, 0, ["squares"], ["const", [1, 6, 9]]]`

- Rule: subtract s ∈ [if n≡0 (mod 5): squares ≤ n else: {1,6,9}]
- game_hash: `32a7af4a639b`
- Tested to N = 1,000,000 in 6.3s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 20, the P-positions are exactly n ≡ 2, 4 (mod 5).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 3, ["squares"], ["const", [8]]]`

- Rule: subtract s ∈ [if n≡3 (mod 8): squares ≤ n else: {8}]
- game_hash: `baa004fd1fd3`
- Tested to N = 1,000,000 in 2.5s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 1, 2, 4, 5, 6, 7 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 4, ["squares"], ["const", [5]]]`

- Rule: subtract s ∈ [if n≡4 (mod 8): squares ≤ n else: {5}]
- game_hash: `20f1ece2b66f`
- Tested to N = 1,000,000 in 2.5s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 16, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 5, ["squares"], ["const", [2]]]`

- Rule: subtract s ∈ [if n≡5 (mod 8): squares ≤ n else: {2}]
- game_hash: `035e044b30f5`
- Tested to N = 1,000,000 in 2.4s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 4, the P-positions are exactly n ≡ 0, 3 (mod 4).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 4, 0, ["squares"], ["const", [8]]]`

- Rule: subtract s ∈ [if n≡0 (mod 4): squares ≤ n else: {8}]
- game_hash: `e74ec10b5ba7`
- Tested to N = 1,000,000 in 4.6s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 1, 2, 3, 5, 6, 7 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 2, ["squares"], ["const", [1, 6, 9]]]`

- Rule: subtract s ∈ [if n≡2 (mod 8): squares ≤ n else: {1,6,9}]
- game_hash: `c5bfed255598`
- Tested to N = 1,000,000 in 2.5s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 35, the P-positions are exactly n ≡ 0, 5, 7, 12, 15, 17, 19, 22, 27, 29 (mod 32).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 2, ["squares"], ["const", [3]]]`

- Rule: subtract s ∈ [if n≡2 (mod 8): squares ≤ n else: {3}]
- game_hash: `31c650a04dd9`
- Tested to N = 1,000,000 in 2.4s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 16, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 4, 3, ["squares"], ["const", [5]]]`

- Rule: subtract s ∈ [if n≡3 (mod 4): squares ≤ n else: {5}]
- game_hash: `bb113155a45c`
- Tested to N = 1,000,000 in 4.5s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 23, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 5, 0, ["squares"], ["range", 3, 6]]`

- Rule: subtract s ∈ [if n≡0 (mod 5): squares ≤ n else: {3..6}]
- game_hash: `f662313dab02`
- Tested to N = 1,000,000 in 3.8s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 20, the P-positions are exactly n ≡ 6, 7, 8 (mod 10).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 1, ["squares"], ["range", 2, 2]]`

- Rule: subtract s ∈ [if n≡1 (mod 8): squares ≤ n else: {2..2}]
- game_hash: `6b5df6a24086`
- Tested to N = 1,000,000 in 2.5s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 3 (mod 4).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 7, 4, ["squares"], ["range", 3, 6]]`

- Rule: subtract s ∈ [if n≡4 (mod 7): squares ≤ n else: {3..6}]
- game_hash: `00d96d1061ac`
- Tested to N = 1,000,000 in 2.8s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 1, 2, 9, 10, 17, 19, 26, 27, 28 (mod 35).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 5, 2, ["squares"], ["const", [6]]]`

- Rule: subtract s ∈ [if n≡2 (mod 5): squares ≤ n else: {6}]
- game_hash: `e25419bc1922`
- Tested to N = 1,000,000 in 3.7s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 17, the P-positions are exactly n ≡ 0, 3 (mod 5).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["squares"], ["const", [1]]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): squares ≤ n else: {1}]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 8.8s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 7, 3, ["squares"], ["const", [3]]]`

- Rule: subtract s ∈ [if n≡3 (mod 7): squares ≤ n else: {3}]
- game_hash: `e6c4b9133f12`
- Tested to N = 1,000,000 in 2.7s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 15, the P-positions are exactly n ≡ 4, 5, 6 (mod 7).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 8, 1, ["squares"], ["const", [7]]]`

- Rule: subtract s ∈ [if n≡1 (mod 8): squares ≤ n else: {7}]
- game_hash: `458fa7c8bcca`
- Tested to N = 1,000,000 in 2.4s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 27, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

---

All laws above are unproven conjectures pending human verification.

---

# Gap closure: every `F / squares` law

Generated by `harsh_verify_gap.py`. Results only; nothing here is proved.

## Why this section exists

The seed=13 sample above drew 20 of 20 games from the `squares / F` branch
order, so no `F / squares` law was tested at 10^6. This section covers **all**
**14** of them — a census of that branch order, not a sample.

## Method

Identical to the section above: `win_loss_flags` and `first_counterexample`
are imported from `harsh_verify.py`, not re-implemented. Win/loss bits only,
move sets from `compile_rule`, early exit on the first winning move, no
Grundy values, tested to N = 1,000,000. No OEIS calls.

## Result

- Laws holding to 10^6: **14 / 14**
- Laws with a counterexample below 10^6: **0 / 14**

| DSL | m | r | F | modulus | residues | preperiod | result |
|---|---:|---:|---|---:|---|---:|---|
| `["ifmod", 2, 0, ["const", [1]], ["squares"]]` | 2 | 0 | `{1}` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [3]], ["squares"]]` | 2 | 0 | `{3}` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [5]], ["squares"]]` | 2 | 0 | `{5}` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [7]], ["squares"]]` | 2 | 0 | `{7}` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [9]], ["squares"]]` | 2 | 0 | `{9}` | 2 | 0 | 1 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [1]], ["squares"]]` | 2 | 1 | `{1}` | 2 | 1 | 4 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [3]], ["squares"]]` | 2 | 1 | `{3}` | 2 | 1 | 4 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [5]], ["squares"]]` | 2 | 1 | `{5}` | 2 | 1 | 12 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [7]], ["squares"]]` | 2 | 1 | `{7}` | 2 | 1 | 16 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [9]], ["squares"]]` | 2 | 1 | `{9}` | 2 | 1 | 10 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [3, 8, 9]], ["squares"]]` | 2 | 0 | `{3,8,9}` | 16 | 0, 2, 4, 6 | 1 | holds to 10^6 |
| `["ifmod", 2, 0, ["const", [8]], ["squares"]]` | 2 | 0 | `{8}` | 16 | 0, 2, 4, 6 | 1 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [3, 8, 9]], ["squares"]]` | 2 | 1 | `{3,8,9}` | 16 | 1, 5, 7, 11 | 1 | holds to 10^6 |
| `["ifmod", 2, 1, ["const", [8]], ["squares"]]` | 2 | 1 | `{8}` | 16 | 1, 3, 5, 7 | 1 | holds to 10^6 |

## Per game

### `["ifmod", 2, 0, ["const", [1]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {1} else: squares ≤ n]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [3]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {3} else: squares ≤ n]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [5]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {5} else: squares ≤ n]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [7]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {7} else: squares ≤ n]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 12.3s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [9]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {9} else: squares ≤ n]
- game_hash: `d124d5dd2614`
- Tested to N = 1,000,000 in 9.3s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [1]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {1} else: squares ≤ n]
- game_hash: `2e161ebc29c9`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 4, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [3]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {3} else: squares ≤ n]
- game_hash: `4cead9445859`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 4, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [5]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {5} else: squares ≤ n]
- game_hash: `d021e9a3b98a`
- Tested to N = 1,000,000 in 9.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 12, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [7]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {7} else: squares ≤ n]
- game_hash: `70874301fd99`
- Tested to N = 1,000,000 in 9.2s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 16, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [9]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {9} else: squares ≤ n]
- game_hash: `aa12e54fa819`
- Tested to N = 1,000,000 in 10.6s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 10, the P-positions are exactly n ≡ 1 (mod 2).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [3, 8, 9]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {3,8,9} else: squares ≤ n]
- game_hash: `337a44d9c5f2`
- Tested to N = 1,000,000 in 10.7s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 2, 4, 6 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 0, ["const", [8]], ["squares"]]`

- Rule: subtract s ∈ [if n≡0 (mod 2): {8} else: squares ≤ n]
- game_hash: `85726ded2ae7`
- Tested to N = 1,000,000 in 9.6s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 0, 2, 4, 6 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [3, 8, 9]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {3,8,9} else: squares ≤ n]
- game_hash: `461813d856f8`
- Tested to N = 1,000,000 in 10.1s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 1, 5, 7, 11 (mod 16).
```

**holds to 10^6** — no counterexample found.

### `["ifmod", 2, 1, ["const", [8]], ["squares"]]`

- Rule: subtract s ∈ [if n≡1 (mod 2): {8} else: squares ≤ n]
- game_hash: `59db46dacd80`
- Tested to N = 1,000,000 in 10.4s

Census conjecture line, reproduced verbatim:

```
CONJECTURE: for n ≥ 1, the P-positions are exactly n ≡ 1, 3, 5, 7 (mod 16).
```

**holds to 10^6** — no counterexample found.

---

All laws above are unproven conjectures pending human verification.
